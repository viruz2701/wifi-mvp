# WiFi Auth Platform MVP

## Описание
Платформа для SMS-авторизации и маркетингового анализа Wi-Fi-клиентов в гостевых сетях.

## Быстрый старт

1. Скопируйте `.env.example` в `.env` и заполните переменные:
   ```bash
   cp .env.example .env