apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: timescaledb:5432
    database: wifi_auth
    user: wifi_user
    secureJsonData:
      password: ${POSTGRES_PASSWORD}
    jsonData:
      sslmode: disable